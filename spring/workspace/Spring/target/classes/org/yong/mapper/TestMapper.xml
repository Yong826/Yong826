<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="org.yong.mapper.TestMapper">
  <!-- public String getTime();  id 에는 'get time' resultType 에는 String 을 쓰면 된다. -->
  <select id="getTime" resultType="String">
    select sysdate();
  </select>
</mapper>